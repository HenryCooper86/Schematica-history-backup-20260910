These original synthetic fixtures contain only literal test markers invented for Schematica. Reproduce byte-for-byte using `python3 tests/fixtures/documents/generate.py` (Python standard library). No user files or third-party document content were used. PDF fixtures cover selectable English and Chinese Type0/CMap text, graphics without text (OCR unavailable), a 101-page limit, and corruption. DOCX is a minimal original OOXML ZIP with English and Chinese plaintext plus a corrupt sample.
